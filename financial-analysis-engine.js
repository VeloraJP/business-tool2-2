import { COST_CLASSIFICATIONS } from "../config/constants.js";

export const STANDARD_COST_CLASSIFICATIONS = Object.freeze({
  PL002: COST_CLASSIFICATIONS.VARIABLE,
  PL004: COST_CLASSIFICATIONS.FIXED,
  PL005: COST_CLASSIFICATIONS.FIXED,
  PL006: COST_CLASSIFICATIONS.FIXED,
  PL008: COST_CLASSIFICATIONS.FIXED,
  PL010: COST_CLASSIFICATIONS.FIXED,
  PL011: COST_CLASSIFICATIONS.FIXED,
  PL012: COST_CLASSIFICATIONS.FIXED,
  PL013: COST_CLASSIFICATIONS.FIXED,
  PL014: COST_CLASSIFICATIONS.FIXED,
  PL015: COST_CLASSIFICATIONS.VARIABLE
});

export const COST_CLASSIFICATION_EXCLUSIONS = Object.freeze({
  PL009: "PL008の内数であり、別分類・別加算しない"
});

