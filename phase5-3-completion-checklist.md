# Phase5.2 レビュー結果

判定：合格

- ホームは会社データ入力を要求せず、知りたいテーマから開始できる。
- 簡単シミュレーションはモード選択後に大きな選択カードを畳み、最低限入力を先に示す。
- 任意入力、詳細条件、計算根拠、入力元は初期状態で折りたたまれる。
- 結果は数値だけでなく、意味、前提条件、次の操作を順に案内する。
- 会社データは主要項目を先に示し、その他と自動計算項目を折りたたむ。
- Error、Warning、不足項目は色と見出しを分け、件数ではなく利用者向け文章で表示する。
- PC・スマートフォンとも主要操作に支障となる崩れはない。
- 禁止された計算・保存契約および凍結仕様への変更はない。

未解決のPhase5.2必須課題：なし。
