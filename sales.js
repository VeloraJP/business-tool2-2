import * as formula from "../formulas/labor-cost.js";
import { calculateMonetaryNumbers, calculateNumbers } from "./validation.js";

export const calculateLaborCostTotal = (executiveCompensation, salaries, statutoryBenefits) => calculateMonetaryNumbers(formula.laborCostTotal, { executiveCompensation, salaries, statutoryBenefits }, { executiveCompensation: { nonNegative: true }, salaries: { nonNegative: true }, statutoryBenefits: { nonNegative: true } });
export const calculateLaborCostRate = (laborCosts, salesAmount) => calculateNumbers(formula.laborCostRate, { laborCosts, salesAmount }, { laborCosts: { nonNegative: true }, salesAmount: { positive: true } });
export const calculateAdditionalSalary = (expectedSalaryPerPerson, plannedHireCount) => calculateMonetaryNumbers(formula.additionalSalary, { expectedSalaryPerPerson, plannedHireCount }, { expectedSalaryPerPerson: { nonNegative: true }, plannedHireCount: { integer: true, nonNegative: true } });
export const calculateSocialInsuranceEstimate = (additionalSalaryAmount, rate) => calculateMonetaryNumbers(formula.socialInsuranceEstimate, { additionalSalaryAmount, rate }, { additionalSalaryAmount: { nonNegative: true }, rate: { rate: true } });
export const calculateAdditionalLaborCost = (additionalSalaryAmount, socialInsuranceAmount) => calculateMonetaryNumbers(formula.additionalLaborCost, { additionalSalaryAmount, socialInsuranceAmount }, { additionalSalaryAmount: { nonNegative: true }, socialInsuranceAmount: { nonNegative: true } });
export const calculateLaborCostAfterHiring = (currentLaborCosts, additionalLaborCosts) => calculateMonetaryNumbers(formula.laborCostAfterHiring, { currentLaborCosts, additionalLaborCosts }, { currentLaborCosts: { nonNegative: true }, additionalLaborCosts: { nonNegative: true } });
export const calculateOperatingProfitAfterHiring = (currentOperatingProfit, expectedAdditionalSales, marginalProfitRateValue, additionalLaborCosts) => calculateMonetaryNumbers(formula.operatingProfitAfterHiring, { currentOperatingProfit, expectedAdditionalSales, marginalProfitRateValue, additionalLaborCosts }, { expectedAdditionalSales: { nonNegative: true }, marginalProfitRateValue: { rate: true }, additionalLaborCosts: { nonNegative: true } });
export const calculateRequiredAdditionalSales = (additionalLaborCosts, marginalProfitRateValue) => calculateMonetaryNumbers(formula.requiredAdditionalSales, { additionalLaborCosts, marginalProfitRateValue }, { additionalLaborCosts: { nonNegative: true }, marginalProfitRateValue: { positive: true } });
export const calculateSalesPerEmployee = (salesAmount, employeeCount) => calculateMonetaryNumbers(formula.salesPerEmployee, { salesAmount, employeeCount }, { salesAmount: { nonNegative: true }, employeeCount: { integer: true, positive: true } });
